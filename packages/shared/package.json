{
    "name": "shared",
    "private": true,
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "devDependencies": {
        "rimraf": "^6.1.3",
        "typescript": "~6.0.3"
    },
    "scripts": {
        "build": "rimraf dist && tsc"
    }
}
